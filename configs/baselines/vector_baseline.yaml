# Baseline A: Single Recurrent Latent Vector (256 dim, 1 slot)
model:
  type: "vector"
  latent_slots: 1
  latent_dim: 256
  reasoning_steps: 4
  decoder_hidden_dim: 64
  num_classes: 2

training:
  epochs: 50
  batch_size: 16
  lr: 1.0e-3
  checkpoint_dir: "results/vector_baseline"
